/**
 * Static file mock for Jest.
 * Expo's jest-expo preset handles most RN module mocks automatically;
 * this file is a fallback for any asset imports (images, fonts) that
 * are not intercepted by the preset's moduleNameMapper.
 */
module.exports = 'test-file-stub';
