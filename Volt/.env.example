# Copy to .env and fill in from the Supabase dashboard (project "Volt").
# Both values are client-safe (publishable key); real secrets like
# EXERCISEDB_API_KEY live only in Supabase Edge Function secrets, never here.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
